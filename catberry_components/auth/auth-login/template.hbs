<div class="container">
  <div class="ui center aligned page grid">
    <div class="eight wide column">
      <div class="ui left aligned segment">
        <h4 class="ui dividing header">{{l10n "LOGIN"}}</h4>
        <form class="ui error form small" name="login">
                        <div class="field">
                            <label for="username">{{l10n "USERNAME"}}:</label>
                            <div class="ui icon input">
                                <input type="text" placeholder="{{l10n "USERNAME"}}" name="username" id="username" /> <i class="user icon"></i>

                            </div>
                        </div>
                        <div class="field">
                            <label for="password">{{l10n "PASSWORD"}}:</label>
                            <div class="ui icon input">
                                <input type="password" placeholder="{{l10n "PASSWORD"}}" name="password" id="password" /> <i class="lock icon"></i>

                            </div>
                        </div>
                        <input type="submit" name="submit" class="ui inverted blue button" />
        </form>
                </div>
            </div>
    </div>
</div>